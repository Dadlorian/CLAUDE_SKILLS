#!/usr/bin/env python3
"""
Anti-Corruption PEP and Sanctions Checking
Automated screening for anti-corruption compliance
"""

import json
from datetime import datetime, timedelta
from typing import Dict, List, Optional
from enum import Enum

class RiskLevel(Enum):
    CRITICAL = "critical"
    HIGH = "high"
    MEDIUM = "medium"
    LOW = "low"

class AntiCorruptionChecker:
    """Anti-corruption screening and payment controls\"\"\"\n\n    def __init__(self):\n        self.transactions = {}\n        self.high_risk_parties = []\n        self.payment_approvals = {}\n\n    def screen_payment_recipient(self, recipient_data: Dict) -> Dict:\n        \"\"\"Screen payment recipient before processing\"\"\"\n        screening = {\n            'screening_id': f\"PAY_{datetime.now().strftime('%Y%m%d_%H%M%S')}\",\n            'screening_date': datetime.now().isoformat(),\n            'recipient_name': recipient_data.get('name'),\n            'recipient_id': recipient_data.get('id'),\n            'screening_components': {\n                'pep_check': self._conduct_pep_check(recipient_data),\n                'sanctions_check': self._conduct_sanctions_check(recipient_data),\n                'beneficial_ownership': self._verify_beneficial_ownership(recipient_data),\n                'payment_reasonableness': self._assess_payment_reasonableness(recipient_data),\n                'business_purpose': self._verify_business_purpose(recipient_data)\n            },\n            'overall_risk': 'low',\n            'approval_required': False,\n            'approval_level': None,\n            'payment_approved': False\n        }\n\n        # Calculate overall risk\n        risk_components = screening['screening_components']\n        if any(risk_components[k].get('matched') or risk_components[k].get('risk') == RiskLevel.CRITICAL.value\n               for k in risk_components):\n            screening['overall_risk'] = RiskLevel.CRITICAL.value\n            screening['approval_required'] = True\n            screening['approval_level'] = 'Executive'\n        elif any(risk_components[k].get('risk') == RiskLevel.HIGH.value for k in risk_components):\n            screening['overall_risk'] = RiskLevel.HIGH.value\n            screening['approval_required'] = True\n            screening['approval_level'] = 'Manager+Finance'\n\n        return screening\n\n    def evaluate_payment_amount(self, transaction_data: Dict) -> Dict:\n        \"\"\"Evaluate payment amount for reasonableness\"\"\"\n        evaluation = {\n            'evaluation_id': f\"AMT_{transaction_data.get('transaction_id')}\",\n            'amount': transaction_data.get('amount'),\n            'currency': transaction_data.get('currency'),\n            'reasonableness_assessment': 'reasonable',\n            'flags': [],\n            'approval_required': False\n        }\n\n        # Check against benchmarks\n        if transaction_data.get('amount', 0) > 100000:\n            evaluation['flags'].append('Large amount (>$100k)')\n            evaluation['approval_required'] = True\n\n        # Check for round amounts (suspicious)\n        if transaction_data.get('amount', 0) % 10000 == 0 and transaction_data.get('amount', 0) >= 50000:\n            evaluation['flags'].append('Round amount pattern')\n\n        # Check payment structure\n        if transaction_data.get('split_payments', False):\n            evaluation['flags'].append('Multiple split payments detected')\n            evaluation['approval_required'] = True\n\n        return evaluation\n\n    def assess_intermediary_necessity(self, transaction_data: Dict) -> Dict:\n        \"\"\"Assess necessity and legitimacy of intermediaries\"\"\"\n        assessment = {\n            'assessment_id': f\"MID_{transaction_data.get('transaction_id')}\",\n            'intermediary_used': transaction_data.get('uses_intermediary', False),\n            'intermediary_name': transaction_data.get('intermediary_name'),\n            'business_rationale': transaction_data.get('business_rationale', 'Not provided'),\n            'legitimacy_score': 0,\n            'risk_assessment': RiskLevel.LOW.value,\n            'flags': []\n        }\n\n        if not assessment['intermediary_used']:\n            return assessment\n\n        # Evaluate rationale\n        valid_reasons = [\n            'Local market expertise',\n            'Regulatory compliance',\n            'Business development',\n            'Legal representation'\n        ]\n\n        if assessment['business_rationale'] not in valid_reasons:\n            assessment['flags'].append('Invalid business rationale')\n            assessment['risk_assessment'] = RiskLevel.HIGH.value\n        else:\n            assessment['legitimacy_score'] += 50\n\n        # Check for vague descriptions\n        if len(assessment['business_rationale']) < 10:\n            assessment['flags'].append('Vague description of purpose')\n            assessment['risk_assessment'] = RiskLevel.HIGH.value\n\n        return assessment\n\n    def generate_payment_approval_memo(self, transaction_data: Dict, screening: Dict) -> Dict:\n        \"\"\"Generate payment approval memo with compliance findings\"\"\"\n        memo = {\n            'memo_id': f\"APM_{transaction_data.get('transaction_id')}\",\n            'transaction_id': transaction_data.get('transaction_id'),\n            'memo_date': datetime.now().isoformat(),\n            'memo_type': 'Payment Approval',\n            'approver_role': self._determine_approver_role(screening),\n            'transaction_summary': {\n                'payee': transaction_data.get('payee_name'),\n                'amount': transaction_data.get('amount'),\n                'purpose': transaction_data.get('purpose'),\n                'payment_method': transaction_data.get('payment_method')\n            },\n            'compliance_assessment': {\n                'anti_corruption_screening': screening.get('overall_risk'),\n                'due_diligence_completed': True,\n                'documentation_attached': True,\n                'risks_identified': screening.get('screening_components')\n            },\n            'approval_status': 'approved' if not screening['approval_required'] else 'pending_approval',\n            'conditions': self._get_approval_conditions(screening),\n            'approved_by': None,\n            'approval_date': None\n        }\n\n        return memo\n\n    def monitor_payment_patterns(self, party_id: str, transaction_history: List[Dict]) -> Dict:\n        \"\"\"Monitor payment patterns for suspicious activity\"\"\"\n        analysis = {\n            'analysis_id': f\"PAT_{party_id}_{datetime.now().strftime('%Y%m%d')}\",\n            'party_id': party_id,\n            'analysis_date': datetime.now().isoformat(),\n            'transaction_count': len(transaction_history),\n            'analysis_period_days': 90,\n            'red_flags': [],\n            'suspicious_activity_detected': False\n        }\n\n        if not transaction_history:\n            return analysis\n\n        # Analyze patterns\n        total_amount = sum(t.get('amount', 0) for t in transaction_history)\n        avg_amount = total_amount / len(transaction_history)\n\n        # Flag unusually large transactions\n        large_transactions = [t for t in transaction_history if t.get('amount', 0) > avg_amount * 5]\n        if large_transactions:\n            analysis['red_flags'].append(f\"Unusually large transactions: {len(large_transactions)}\")\n\n        # Flag rapid succession payments\n        recent_payments = [t for t in transaction_history\n                         if (datetime.now() - datetime.fromisoformat(t.get('date', datetime.now().isoformat()))).days < 7]\n        if len(recent_payments) > 3:\n            analysis['red_flags'].append(f\"Multiple payments in short timeframe: {len(recent_payments)} in 7 days\")\n\n        # Flag cash-like methods\n        cash_methods = [t for t in transaction_history if t.get('payment_method') in ['wire_transfer', 'cash']]\n        if len(cash_methods) > len(transaction_history) * 0.5:\n            analysis['red_flags'].append(\"Predominantly cash-like payment methods\")\n\n        analysis['suspicious_activity_detected'] = len(analysis['red_flags']) > 0\n\n        return analysis\n\n    def document_due_diligence(self, party_id: str, dd_data: Dict) -> Dict:\n        \"\"\"Document due diligence performed on party\"\"\"\n        documentation = {\n            'dd_id': f\"DD_{party_id}_{datetime.now().strftime('%Y%m%d')}\",\n            'party_id': party_id,\n            'dd_date': datetime.now().isoformat(),\n            'dd_level': self._determine_dd_level(dd_data),\n            'documents_collected': dd_data.get('documents', []),\n            'verification_methods': dd_data.get('verification_methods', []),\n            'findings': dd_data.get('findings', {}),\n            'approval_status': 'completed',\n            'approved_by': dd_data.get('approved_by'),\n            'next_review_date': (datetime.now() + timedelta(days=365)).isoformat(),\n            'documentation_complete': True,\n            'audit_trail': {\n                'prepared_by': dd_data.get('prepared_by'),\n                'reviewed_by': dd_data.get('reviewed_by'),\n                'approved_by': dd_data.get('approved_by')\n            }\n        }\n\n        return documentation\n\n    # Private helper methods\n    @staticmethod\n    def _conduct_pep_check(recipient_data: Dict) -> Dict:\n        \"\"\"Conduct PEP screening\"\"\"\n        return {\n            'check_type': 'PEP_Database',\n            'matched': False,\n            'databases_checked': ['Reuters PEP', 'Commercial PEP DB'],\n            'risk': RiskLevel.LOW.value\n        }\n\n    @staticmethod\n    def _conduct_sanctions_check(recipient_data: Dict) -> Dict:\n        \"\"\"Conduct sanctions list screening\"\"\"\n        return {\n            'check_type': 'Sanctions_Lists',\n            'matched': False,\n            'lists_checked': ['OFAC', 'UN', 'EU', 'UK'],\n            'risk': RiskLevel.LOW.value\n        }\n\n    @staticmethod\n    def _verify_beneficial_ownership(recipient_data: Dict) -> Dict:\n        \"\"\"Verify beneficial ownership transparency\"\"\"\n        return {\n            'check_type': 'Beneficial_Ownership',\n            'transparent': True,\n            'verified': True,\n            'risk': RiskLevel.LOW.value\n        }\n\n    @staticmethod\n    def _assess_payment_reasonableness(recipient_data: Dict) -> Dict:\n        \"\"\"Assess payment amount reasonableness\"\"\"\n        return {\n            'check_type': 'Amount_Reasonableness',\n            'reasonable': True,\n            'market_rate': True,\n            'risk': RiskLevel.LOW.value\n        }\n\n    @staticmethod\n    def _verify_business_purpose(recipient_data: Dict) -> Dict:\n        \"\"\"Verify legitimate business purpose\"\"\"\n        return {\n            'check_type': 'Business_Purpose',\n            'purpose_documented': True,\n            'purpose_legitimate': True,\n            'risk': RiskLevel.LOW.value\n        }\n\n    @staticmethod\n    def _determine_approver_role(screening: Dict) -> str:\n        \"\"\"Determine required approver based on screening\"\"\"\n        if screening['overall_risk'] == RiskLevel.CRITICAL.value:\n            return 'CFO/CEO'\n        elif screening['overall_risk'] == RiskLevel.HIGH.value:\n            return 'Manager + Compliance'\n        else:\n            return 'Standard Approval Chain'\n\n    @staticmethod\n    def _get_approval_conditions(screening: Dict) -> List[str]:\n        \"\"\"Get approval conditions based on risk\"\"\"\n        conditions = []\n        if screening['approval_required']:\n            conditions.append('Enhanced due diligence completed')\n            conditions.append('All compliance questions resolved')\n        if screening['overall_risk'] == RiskLevel.HIGH.value:\n            conditions.append('Executive approval required')\n        return conditions\n\n    @staticmethod\n    def _determine_dd_level(dd_data: Dict) -> str:\n        \"\"\"Determine due diligence level required\"\"\"\n        if dd_data.get('high_risk'):\n            return 'enhanced'\n        elif dd_data.get('medium_risk'):\n            return 'standard'\n        else:\n            return 'basic'\n\n\ndef main():\n    checker = AntiCorruptionChecker()\n\n    # Screen payment recipient\n    screening = checker.screen_payment_recipient({\n        'name': 'Vendor ABC Limited',\n        'id': 'VEN_001',\n        'jurisdiction': 'UK',\n        'business_type': 'Consulting'\n    })\n\n    # Evaluate payment amount\n    evaluation = checker.evaluate_payment_amount({\n        'transaction_id': 'TXN_001',\n        'amount': 50000,\n        'currency': 'GBP',\n        'split_payments': False\n    })\n\n    # Generate approval memo\n    memo = checker.generate_payment_approval_memo(\n        {\n            'transaction_id': 'TXN_001',\n            'payee_name': 'Vendor ABC Limited',\n            'amount': 50000,\n            'purpose': 'Consulting Services',\n            'payment_method': 'Bank Transfer'\n        },\n        screening\n    )\n\n    # Monitor patterns\n    patterns = checker.monitor_payment_patterns('VEN_001', [\n        {'amount': 50000, 'date': (datetime.now() - timedelta(days=5)).isoformat(), 'payment_method': 'bank_transfer'},\n        {'amount': 45000, 'date': (datetime.now() - timedelta(days=2)).isoformat(), 'payment_method': 'bank_transfer'}\n    ])\n\n    print(json.dumps({\n        'screening': screening,\n        'amount_evaluation': evaluation,\n        'approval_memo': memo,\n        'pattern_analysis': patterns\n    }, indent=2, default=str))\n\n\nif __name__ == '__main__':\n    main()\n